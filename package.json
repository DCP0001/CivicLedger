{
  "name": "secure-vote-root",
  "version": "1.0.0",
  "description": "Root scripts for running the Secure Blockchain Voting Platform",
  "scripts": {
    "backend:dev": "cd backend && npm run dev",
    "backend:seed": "cd backend && npx ts-node prisma/seed.ts",
    "voter:dev": "cd voter-frontend && npm run dev",
    "voter:build": "cd voter-frontend && npm run build",
    "admin:dev": "cd admin-frontend && npm run dev",
    "admin:build": "cd admin-frontend && npm run build"
  }
}
